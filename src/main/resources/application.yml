spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dbgirl
    data-username: root
    data-password: root
  jpa:
    hibernate:
      ddl-auto: update  #自动更新
      #ddl-auto: create  #每次运行自动创建空表
    show-sql: true
